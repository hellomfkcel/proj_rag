# 外部权限管理系统设计

> **设计目标**：设计一个外部权限管理系统，满足 `docs/RAG系统设计v14.md` 的契约要求，服务 RAG v14 项目的权限管理需要。
>
> **设计依据**：
> - `docs/RAG系统设计v14.md` §6 P-AUTHC + §6A 五端点契约 + §13.7 结构镜像 + §14.5 盖戳管道
> - `docs/权限管理系统架构设计.md` — 四方协作模型
> - `docs/frontend-design.md` §3.7 — 管理台跳转入口规格
> - 项目代码现状：`src/permission/cerbos_client.py` 的本地资源镜像实现

---

## 一、外部系统全景架构

外部系统由**四个子系统**组成，与 RAG v14 本系统形成完整的权限管理闭环：

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                          外部系统全景                                          │
│                                                                              │
│  ┌───────────────────────┐          ┌──────────────────────────────┐         │
│  │   IdP 身份源           │          │   Cerbos PDP 策略决策点       │         │
│  │   (Keycloak)          │          │   (ghcr.io/cerbos/cerbos)    │         │
│  │                       │          │                              │         │
│  │  • 用户 CRUD           │          │  • 策略加载与缓存              │         │
│  │  • 组管理              │          │  • 决策面: /api/check/*      │         │
│  │  • 顶层角色分配         │          │  • 审计日志                   │         │
│  │  • JWT 签发(OIDC)      │          │  • 策略版本管理               │         │
│  │  • 用户属性/claims     │          │                              │         │
│  └───────┬───────────────┘          └──────────────┬───────────────┘         │
│          │                                         │                         │
│          │ JWT/OIDC                                 │ 策略评估                │
│          ▼                                         ▼                         │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                     权限服务后端 (Permission Service)                   │   │
│  │                                                                      │   │
│  │  ┌────────────────────┐  ┌────────────────────┐  ┌────────────────┐  │   │
│  │  │ 决策面 API          │  │ 投影面 API          │  │ 管理面 API      │  │   │
│  │  │ POST /v1/check     │  │ GET /v1/prefilter   │  │ register       │  │   │
│  │  │ POST /v1/filter    │  │ POST /v1/visibility │  │ link/unlink    │  │   │
│  │  │ POST /v1/context   │  │                     │  │ retire         │  │   │
│  │  └────────────────────┘  └────────────────────┘  └────────────────┘  │   │
│  │                                                                      │   │
│  │  数据层:  PostgreSQL (权威 ACL/角色绑定/限制/资源镜像/事件日志)        │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│          │                                                                  │
│          │ 管理 API (REST)                                                   │
│          ▼                                                                  │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                       管理台前端 (Admin Console)                       │   │
│  │                                                                      │   │
│  │  • 资源管理 (KB/文档)    • 权限授予/回收    • 角色绑定管理             │   │
│  │  • 封禁/限制管理         • 策略管理          • 审计日志查询            │   │
│  │  • 用户/组/租户概览      • 模拟判定 (Playground)                       │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  ◄══════════════ RAG v14 系统边界 ══════════════════►                        │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                    RAG v14 本系统（权限消费方）                         │   │
│  │                                                                      │   │
│  │  P-AUTHC ──HTTP──► 权限服务后端（决策面 + 投影面 + 管理面）            │   │
│  │  前端 ──跳转链接──► 管理台前端                                         │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## 二、权限服务后端 (Permission Service) 设计

### 2.1 定位

权限服务后端是 RAG v14 系统与 Cerbos PDP 之间的**中间层服务**，负责：

| 职责 | 说明 |
|------|------|
| **ACL/角色绑定/限制的权威存储** | 这些数据不在 RAG 系统，不在 Cerbos PDP，而在此服务 |
| **包装 Cerbos PDP 决策** | 将 ACL 数据注入 Cerbos principal/resource attr，调 PDP 判定 |
| **提供投影面** | prefilter + visibility 端点——基于 ACL 数据预计算，供 RAG 系统高频调用 |
| **提供管理面** | 生命周期端口 (register/link/unlink/retire) + 管理台 CRUD API |
| **发布 VisibilityChanged 事件** | ACL 变更后通知 RAG 系统刷新盖戳 |
| **ctx_token 铸造与验证** | 替 RAG 系统安全传递 credential 到异步 worker |

### 2.2 技术选型建议

| 层 | 选型 | 理由 |
|----|------|------|
| 语言/框架 | **Python (FastAPI)** 或 **Go (Gin)** | Python: 与 RAG 系统技术栈一致，团队可维护；Go: 高性能、低资源、适合高并发投影查询 |
| 数据库 | **PostgreSQL** | 与 RAG 系统相同，运维统一；ACL/角色绑定数据需要事务一致性 |
| PDP | **Cerbos PDP** (已有) | 策略评估引擎，通过 HTTP/gRPC 调用 |
| 事件发布 | **Redis Pub/Sub** 或 **PostgreSQL Outbox** | RAG 系统已用 Redis，保持技术栈一致 |
| 部署 | Docker Compose（开发）/ K8s（生产） | 与 RAG 系统共用基础设施 |

**推荐选型**：Python (FastAPI) + PostgreSQL + Redis Pub/Sub，理由是与 RAG 系统技术栈完全一致，降低运维复杂度。

### 2.3 数据模型

#### 2.3.1 核心表设计

```sql
-- ═══════════════════════════════════════════════════
-- 资源注册表（结构镜像的权威源）
-- ═══════════════════════════════════════════════════
CREATE TABLE resource_registry (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    resource_type VARCHAR(64)  NOT NULL,        -- 'kb' | 'document' | 'directory'
    resource_id   VARCHAR(255) NOT NULL,        -- RAG 系统中的资源 ID
    tenant_id     VARCHAR(255) NOT NULL,        -- 所属租户
    owner         VARCHAR(255) NOT NULL,        -- 'user:xxx' 格式
    retired       BOOLEAN NOT NULL DEFAULT false,
    created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
    
    UNIQUE (resource_type, resource_id)
);

-- ═══════════════════════════════════════════════════
-- 挂载关系表
-- ═══════════════════════════════════════════════════
CREATE TABLE mount_registry (
    id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    doc_id     VARCHAR(255) NOT NULL,
    kb_id      VARCHAR(255) NOT NULL,
    unlinked   BOOLEAN NOT NULL DEFAULT false,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    
    UNIQUE (doc_id, kb_id)
);

-- ═══════════════════════════════════════════════════
-- ACL 表 (Access Control List) —— 权限授予记录
-- ═══════════════════════════════════════════════════
CREATE TABLE acl_entries (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id     VARCHAR(255) NOT NULL,
    principal     VARCHAR(255) NOT NULL,        -- 'user:alice' | 'group:eng' | 'role:viewer'
    resource_type VARCHAR(64)  NOT NULL,        -- 'kb' | 'document'
    resource_id   VARCHAR(255) NOT NULL,        -- 资源 ID
    action        VARCHAR(64)  NOT NULL,        -- 'kb:read' | 'doc:view' | 'doc:download' | ...
    granted_by    VARCHAR(255) NOT NULL,        -- 授予者
    granted_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
    expires_at    TIMESTAMPTZ,                  -- 可选的过期时间
    revoked       BOOLEAN NOT NULL DEFAULT false,
    revoked_at    TIMESTAMPTZ,
    
    -- 一个 principal 对同一资源的同一 action 只应有一条活跃记录
    UNIQUE (principal, resource_type, resource_id, action)
);
CREATE INDEX idx_acl_resource ON acl_entries(resource_type, resource_id) WHERE NOT revoked;
CREATE INDEX idx_acl_principal ON acl_entries(principal) WHERE NOT revoked;
CREATE INDEX idx_acl_tenant ON acl_entries(tenant_id);

-- ═══════════════════════════════════════════════════
-- 角色绑定表
-- ═══════════════════════════════════════════════════
CREATE TABLE role_bindings (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id     VARCHAR(255) NOT NULL,
    principal     VARCHAR(255) NOT NULL,        -- 'user:alice' | 'group:eng'
    role          VARCHAR(64)  NOT NULL,        -- 'kb_reader' | 'kb_writer' | 'kb_admin' | 'admin'
    resource_type VARCHAR(64),                  -- 可选：限定到特定资源类型的 KB
    resource_id   VARCHAR(255),                 -- 可选：限定到特定 KB
    granted_by    VARCHAR(255) NOT NULL,
    granted_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
    revoked       BOOLEAN NOT NULL DEFAULT false,
    revoked_at    TIMESTAMPTZ,
    
    UNIQUE (principal, role, resource_type, COALESCE(resource_id, ''))
);

-- ═══════════════════════════════════════════════════
-- 限制表（封禁/阻断规则）
-- ═══════════════════════════════════════════════════
CREATE TABLE restrictions (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id       VARCHAR(255) NOT NULL,
    restriction_type VARCHAR(64) NOT NULL,       -- 'subject_ban' (型一) | 'resource_restriction' (型二)
    principal       VARCHAR(255),                -- 型一：被封禁的主体
    resource_type   VARCHAR(64),                 -- 型二：受限的资源类型
    resource_id     VARCHAR(255),                -- 型二：受限的资源 ID
    reason          TEXT,
    created_by      VARCHAR(255) NOT NULL,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
    removed         BOOLEAN NOT NULL DEFAULT false,
    removed_at      TIMESTAMPTZ,
    
    -- 型一封禁：principal 必须非空
    -- 型二限制：resource_type + resource_id 必须非空
    CONSTRAINT ck_restriction_type CHECK (
        (restriction_type = 'subject_ban' AND principal IS NOT NULL) OR
        (restriction_type = 'resource_restriction' AND resource_type IS NOT NULL)
    )
);

-- ═══════════════════════════════════════════════════
-- 变更事件日志表（用于对账 + 审计）
-- ═══════════════════════════════════════════════════
CREATE TABLE permission_changes (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    event_type    VARCHAR(128) NOT NULL,        -- 'ACL_GRANTED' | 'ACL_REVOKED' | 'ROLE_BOUND' | 'RESTRICTION_ADDED' | ...
    resource_type VARCHAR(64),
    resource_id   VARCHAR(255),
    kb_id         VARCHAR(255),                 -- 用于 KB 粒度事件展开
    tenant_id     VARCHAR(255) NOT NULL,
    change_detail JSONB NOT NULL,               -- 变更详情
    version       BIGINT NOT NULL,              -- 全局单调递增版本号
    created_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX idx_pc_resource ON permission_changes(resource_type, resource_id);
CREATE INDEX idx_pc_kb ON permission_changes(kb_id);
CREATE INDEX idx_pc_version ON permission_changes(version);
```

#### 2.3.2 全局版本号

```sql
-- 用于 prefilter 的 policy_version 和 visibility 的 version
CREATE SEQUENCE global_permission_version START 1;

-- 每次 ACL/角色绑定/限制变更时，递增此序列
-- 变更事件记录当前版本号
```

### 2.4 API 设计

#### 2.4.1 决策面 API（RAG 系统 P-AUTHC 调用）

```
┌─────────────────────────────────────────────────────────────────────┐
│ POST /v1/check                                                      │
├─────────────────────────────────────────────────────────────────────┤
│ 调用方: P-AUTHC check()                                             │
│ Client-ID: interactive-backend                                      │
│                                                                     │
│ Request:                                                            │
│   {                                                                 │
│     "request_id": "abc123...",                                      │
│     "credential": "<JWT raw>",                                      │
│     "action": "kb:write",                                           │
│     "resource": {"type": "kb", "id": "kb-uuid"},                    │
│     "channel": {"kb": "kb-uuid"}   // 通道类动词必带                 │
│   }                                                                 │
│                                                                     │
│ 后端处理:                                                           │
│   1. 从 JWT 解析 principal (user_id, roles, groups)                 │
│   2. 查询 ACL 表: 该 principal 是否有此 action?                      │
│   3. 查询 restrictions 表: 该主体/资源是否被封禁?                    │
│   4. 构造 Cerbos principal (含 resolved permissions)                │
│   5. 调用 Cerbos /api/check/resources                               │
│   6. 三态映射: EFFECT_ALLOW→allow, DENY→deny, 其他→indeterminate    │
│                                                                     │
│ Response:                                                           │
│   {                                                                 │
│     "decision": "allow" | "deny" | "indeterminate",                 │
│     "decision_id": "cerbos-call-id-xxx",                            │
│     "reasons": [],                                                  │
│     "obligations": {}                                               │
│   }                                                                 │
│                                                                     │
│ 性能目标: p95 < 50ms                                                 │
│ 限流: 每 client 1000 req/s                                          │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ POST /v1/filter                                                      │
├─────────────────────────────────────────────────────────────────────┤
│ 调用方: P-AUTHC filter_items() (仅 strict KB, 层3)                   │
│ Client-ID: retrieval                                                 │
│                                                                     │
│ Request:                                                            │
│   {                                                                 │
│     "request_id": "...",                                            │
│     "credential": "<JWT raw 或 ctx_token>",                         │
│     "items": [                                                      │
│       {"resource_type": "document", "resource_id": "doc-1",         │
│        "channel": {"kb": "kb-uuid"}},                               │
│       ...                                                           │
│     ]  // ≤200 条                                                   │
│   }                                                                 │
│                                                                     │
│ 后端处理:                                                           │
│   对每条 item 执行 doc:retrieve 判定                                  │
│   → 批量调 Cerbos，单次往返                                          │
│                                                                     │
│ Response:                                                           │
│   {                                                                 │
│     "allowed": ["doc-1", "doc-3"],                                  │
│     "denied":  ["doc-2"],                                           │
│     "decision_id": "..."                                            │
│   }                                                                 │
│                                                                     │
│ 约束: 单批 ≤200; 超时/失败 → RAG 侧整批 deny; 永久禁止缓存          │
└─────────────────────────────────────────────────────────────────────┘
```

#### 2.4.2 投影面 API

```
┌─────────────────────────────────────────────────────────────────────┐
│ GET /v1/prefilter?credential=<JWT>                                  │
├─────────────────────────────────────────────────────────────────────┤
│ 调用方: P-AUTHC get_prefilter() (每次检索前)                         │
│ Client-ID: retrieval                                                 │
│                                                                     │
│ 后端处理:                                                           │
│   1. 从 JWT 解析 principal                                          │
│   2. 查询 restrictions: 该主体是否被型一封禁?                          │
│      → 是: 返回 {suspended: true}                                   │
│   3. 查询 ACL + role_bindings:                                      │
│      该 principal 在哪些 KB 上有 kb:read 或更强权限?                  │
│   4. 查询 resource_registry: 过滤 retired=false 的资源               │
│   5. 计算 kbs (允许列表) 和 excluded_kbs (型二封禁的资源)            │
│                                                                     │
│ Response:                                                           │
│   {                                                                 │
│     "kbs": ["kb-uuid-1", "kb-uuid-3"],                              │
│     "excluded_kbs": [],                                             │
│     "tenant_wide_read": false,                                      │
│     "policy_version": "v12345",          // = 全局版本号              │
│     "ttl_s": 60,                        // 请求内缓存 TTL            │
│     "expires_at": "2026-07-30T12:01:00Z"                            │
│   }                                                                 │
│                                                                     │
│ 或 (型一封禁):                                                       │
│   {                                                                 │
│     "suspended": true,                                              │
│     "reason": "subject_banned"                                      │
│   }                                                                 │
│                                                                     │
│ 性能目标: p95 < 30ms (高频调用)                                      │
│ 缓存: RAG 侧请求内缓存 (不跨请求)，TTL ≤60s                          │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ POST /v1/visibility                                                  │
├─────────────────────────────────────────────────────────────────────┤
│ 调用方: B-INGEST 盖戳管道 (stamp_channel_task)                       │
│ Client-ID: ingest (无主体入参)                                       │
│                                                                     │
│ Request:                                                            │
│   {                                                                 │
│     "tenant": "tenant-dev",                                         │
│     "doc_id": "doc-uuid",                                           │
│     "channel": {"kb": "kb-uuid"}                                    │
│   }                                                                 │
│                                                                     │
│ 后端处理:                                                           │
│   1. 查 mount_registry: 挂载是否存在? 是否已 unlinked?               │
│      → unlinked: 返回 {unmounted: true}                             │
│   2. 查 resource_registry: doc/kb 是否 retired?                     │
│      → retired: 返回 {unmounted: true}                              │
│   3. 查 ACL + role_bindings:                                        │
│      哪些 principal 对此 (doc,kb) 有 doc:retrieve 权限?              │
│      → 计算 allow_stamps 列表                                       │
│   4. 查 restrictions: 哪些 principal 被禁止访问?                     │
│      → 计算 deny_stamps 列表                                        │
│                                                                     │
│ Response:                                                           │
│   {                                                                 │
│     "allow_stamps": ["user:*", "group:eng", "user:bob"],            │
│     "deny_stamps":  ["user:mallory"],                               │
│     "version": 12345,                      // = 全局权限版本号       │
│     "unmounted": false                                              │
│   }                                                                 │
│                                                                     │
│ 关键约束:                                                            │
│  • allow_stamps/deny_stamps 只含原始主体，禁止展开成员               │
│  • version 用于 B-INGEST 版本单调性检查                               │
│  • 此端点无主体入参 — 返回的是该资源的"可见性投影"                   │
│  • 被限流(429) → 返回 Retry-After 头                                │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ POST /v1/context                                                     │
├─────────────────────────────────────────────────────────────────────┤
│ 调用方: P-AUTHC mint_ctx_token()                                     │
│ Client-ID: interactive-backend                                       │
│                                                                     │
│ Request:                                                            │
│   {                                                                 │
│     "request_id": "...",                                            │
│     "credential": "<JWT raw>",                                      │
│     "audience": "retrieval-worker",                                 │
│     "ttl_s": 600                                                    │
│   }                                                                 │
│                                                                     │
│ 后端处理:                                                           │
│   将 credential + audience + iat + exp 打包为签名 token              │
│   (HMAC-SHA256 或 JWT 自签)                                          │
│                                                                     │
│ Response:                                                           │
│   {                                                                 │
│     "ctx_token": "ctx.eyJjcmVkZW50aWFsIjoi...xyz",                  │
│     "expires_at": "2026-07-30T12:10:00Z"                            │
│   }                                                                 │
│                                                                     │
│ 约束: ttl_s ≤ 600; audience 必须校验; 过期即失效                     │
└─────────────────────────────────────────────────────────────────────┘
```

#### 2.4.3 管理面 API — 生命周期端口（RAG 系统 B-DOC 调用）

```
┌──────────────────────────────────────────────────────────────────────┐
│ POST /v1/resources/register                                          │
├──────────────────────────────────────────────────────────────────────┤
│ 调用方: B-DOC (创建 document / 创建 KB 时)                            │
│                                                                      │
│ Request:                                                             │
│   {                                                                  │
│     "resource_type": "kb",                                           │
│     "resource_id": "kb-uuid",                                        │
│     "owner": "user:admin",                                           │
│     "tenant_id": "tenant-dev",                                       │
│     "idempotency_key": "reg-kb-kb-uuid-ts123"  // 必带               │
│   }                                                                  │
│                                                                      │
│ Response (200):                                                      │
│   { "change_id": "...", "result": "created" | "noop" }               │
│                                                                      │
│ Response (409):                                                      │
│   { "error": "idempotency_conflict",                                 │
│     "message": "same key, different payload" }                       │
│                                                                      │
│ 调用纪律:                                                             │
│  • idempotency_key 格式: {facade}-{tenant}-{resource_id}-{schema_v}  │
│  • 禁止时间戳/随机数/UUID                                            │
│  • result: "noop" 是成功                                             │
│  • RAG 侧调用失败 → 回滚本地事务                                      │
└──────────────────────────────────────────────────────────────────────┘

POST /v1/resources/link     -- 挂载建立 (doc_id, kb_id)
POST /v1/resources/unlink   -- 解除挂载 (doc_id, kb_id)
POST /v1/resources/retire   -- 资源退役 (resource_type, resource_id)
    ↑ 四个端点的请求/响应格式与 register 同构
```

#### 2.4.4 管理面 API — 管理台专用（RAG 系统不调）

```
这些是管理台前端调用的 API，RAG 系统不感知：

┌──────────────────────────────────────────────────────────────────┐
│ ACL 管理                                                         │
├──────────────────────────────────────────────────────────────────┤
│ POST   /api/v1/acl/grant         授予权限                        │
│ POST   /api/v1/acl/revoke        回收权限                        │
│ POST   /api/v1/acl/batch-grant   批量授予                        │
│ GET    /api/v1/acl?resource_type=&resource_id=  查询 ACL 列表    │
│ GET    /api/v1/acl/effective?principal=&resource_type=&resource_id= │
│        ↑ 计算某主体对某资源的有效权限 (合并 ACL + 角色 + 限制)     │
├──────────────────────────────────────────────────────────────────┤
│ 角色绑定管理                                                      │
├──────────────────────────────────────────────────────────────────┤
│ POST   /api/v1/roles/bind        绑定角色                        │
│ POST   /api/v1/roles/unbind      解除绑定                        │
│ GET    /api/v1/roles/bindings?principal=&resource_type=&resource_id= │
├──────────────────────────────────────────────────────────────────┤
│ 限制管理                                                          │
├──────────────────────────────────────────────────────────────────┤
│ POST   /api/v1/restrictions/add      添加封禁/限制                │
│ POST   /api/v1/restrictions/remove   解除封禁/限制                │
│ GET    /api/v1/restrictions?principal=&resource_type=&resource_id= │
├──────────────────────────────────────────────────────────────────┤
│ 资源管理                                                          │
├──────────────────────────────────────────────────────────────────┤
│ GET    /api/v1/resources?type=kb&tenant_id=   列出资源            │
│ GET    /api/v1/resources/{type}/{id}/owners   查看资源所有权      │
│ POST   /api/v1/resources/transfer-ownership   转移所有权          │
├──────────────────────────────────────────────────────────────────┤
│ 审计与模拟                                                        │
├──────────────────────────────────────────────────────────────────┤
│ GET    /api/v1/audit?resource_type=&resource_id=&principal=&from=&to= │
│        ↑ 权限变更历史查询                                        │
│ POST   /api/v1/simulate               模拟权限判定 (Playground)    │
│        {principal, action, resource} → {decision, reasons}        │
└──────────────────────────────────────────────────────────────────┘
```

### 2.5 核心业务流程

#### 2.5.1 权限判定 (check)

```
P-AUTHC ──POST /v1/check──► 权限服务后端
                                │
                                │ 1. 解析 JWT → principal
                                │ 2. 查询 ACL + role_bindings + restrictions
                                │    → 构建 Cerbos principal 的 attr.granted_actions
                                │ 3. 查询 resource_registry → 构建 Cerbos resource 的 attr
                                │ 4. POST Cerbos /api/check/resources
                                │ 5. 映射 Cerbos verdict → 三态
                                │
                                ◄── {decision, decision_id, reasons}
```

#### 2.5.2 可见性投影 (visibility)

```
B-INGEST ──POST /v1/visibility──► 权限服务后端
                                      │
                                      │ 1. 查 mount_registry → unlinked?
                                      │ 2. 查 resource_registry → retired?
                                      │ 3. 查 ACL: 哪些 principal 对该 (doc,kb) 有 doc:retrieve?
                                      │    → allow_stamps 列表
                                      │ 4. 查 restrictions: 哪些 principal 被禁止?
                                      │    → deny_stamps 列表
                                      │ 5. 取当前全局版本号 → version
                                      │
                                      ◄── {allow_stamps, deny_stamps, version, unmounted}
```

#### 2.5.3 权限变更 → 事件发布

```
管理台 ──POST /api/v1/acl/grant──► 权限服务后端
                                       │
                                       │ 1. INSERT acl_entries
                                       │ 2. 递增 global_permission_version
                                       │ 3. INSERT permission_changes (version=N)
                                       │ 4. 发布 Redis Pub/Sub:
                                       │    channel: "visibility_changed"
                                       │    payload: {event_type, resource_type, resource_id,
                                       │               kb_id?, tenant_id, version, change_detail}
                                       │
                                       ▼
                                  Redis Pub/Sub "visibility_changed"
                                       │
                                       ▼
                                  P-AUTHC (visibility_events.py 订阅)
                                       │
                                       ▼
                                  B-INGEST stamp_channel_task
```

### 2.6 与 RAG 系统当前实现的对照

当前 RAG 系统 `cerbos_client.py` 中的实现方式 vs 外部权限服务：

| 功能 | 当前实现 (开发模式) | 外部权限服务 (生产模式) |
|------|-------------------|----------------------|
| **check** | 直接调用 Cerbos HTTP API，principal.granted_actions 从本地 resource_registry 反查 | 调用权限服务后端 `/v1/check`，ACL 数据由权限服务管理 |
| **check_batch** | 直接调用 Cerbos /api/check/resources 批量 | 同上，经权限服务后端 |
| **filter_items** | 直接调用 Cerbos，逐 200 条分批 | 调用权限服务后端 `/v1/filter` |
| **get_prefilter** | 查本地 DB knowledge_bases → Cerbos 批量 check kb:read | 调用权限服务后端 `GET /v1/prefilter` |
| **get_visibility** | 查本地 mirror 表 (resource_registry + mount_registry + document_kb_mounts) | 调用权限服务后端 `POST /v1/visibility` |
| **mint_ctx_token** | 本地 HMAC-SHA256 自签 | 调用权限服务后端 `POST /v1/context` |
| **register/link/unlink/retire** | 写本地 mirror 表 (resource_registry / mount_registry) | 调用权限服务后端对应端点 |
| **ACL 管理** | 无（开发模式: owner = full access） | 管理台 → 权限服务后端 API |
| **VisibilityChanged** | 本地 visibility_events.py 轮询 mount_registry | 权限服务 Redis Pub/Sub → P-AUTHC 订阅 |

**迁移策略**：`cerbos_client.py` 中的 `CerbosClient` 类改造为 HTTP 客户端，将当前直接操作本地 DB 的逻辑替换为对权限服务后端的 HTTP 调用。P-AUTHC 模块 (`authz.py`) 的接口签名不变——这是防腐层的价值。

---

## 三、管理台前端 (Admin Console) 设计

### 3.1 定位

管理台是权限管理员使用的 Web 应用。RAG v14 前端通过 `ADMIN_CONSOLE_URL` 环境变量配置的跳转链接指向管理台。

### 3.2 技术选型建议

| 层 | 选型 | 理由 |
|----|------|------|
| 框架 | **Next.js 14** 或 **Vue 3 + Vite** | 与 RAG 前端技术栈一致用 Next.js；或用更轻量的 Vue 3 |
| 组件库 | **shadcn/ui** 或 **Ant Design** | shadcn/ui 与 RAG 前端统一视觉风格；Ant Design 有成熟的表格/表单/权限管理组件 |
| 状态管理 | Zustand 或 Pinia | 轻量 |
| API 调用 | Axios (与 RAG 前端一致) | — |

**推荐**：Next.js 14 + shadcn/ui + Zustand，与 RAG 前端保持技术栈一致。如有成熟的 Ant Design Pro 权限管理模板，也可优先复用。

### 3.3 页面结构

```
管理台 (Admin Console)
│
├── 🔐 /login                    → 管理台独立登录 (管理员 SSO)
│
├── 📊 /dashboard                → 管理台首页
│   ├── 权限概览统计              → KB 数、文档数、用户数、ACL 条目数
│   ├── 最近变更时间线            → 近期权限授予/回收/封禁记录
│   └── 待处理告警               → 孤儿权限、过期 ACL、异常权限
│
├── 📁 /resources                → 资源管理
│   ├── KB 列表                   → DataTable (名称、文档数、ACL 条目数、创建时间)
│   │   └── 点击进入 KB 详情      → 该 KB 的 ACL 列表 + 角色绑定 + 文档列表
│   ├── 文档列表                  → DataTable (按 KB 筛选、按租户筛选)
│   │   └── 点击进入文档详情      → 该文档的 ACL 列表 + 权限历史
│   └── 搜索 (按名称/ID/租户)     → 全局搜索
│
├── 👥 /users-groups             → 用户与组管理 (只读视图，数据来自 IdP)
│   ├── 用户列表                  → 同步自 Keycloak: 用户名、邮箱、所属组、租户
│   ├── 组列表                    → 同步自 Keycloak: 组名、成员数
│   └── 用户/组详情              → 拥有的权限汇总 (从 ACL + 角色绑定聚合)
│
├── 🔑 /permissions              → 权限管理 (核心页面)
│   ├── 授予权限 Dialog            → 选择 principal + resource + action + 过期时间
│   ├── 回收权限                  → 逐条或批量 revoke
│   ├── 角色绑定                   → 选择 principal + role (+ 可选 resource scope)
│   ├── 批量操作                   → CSV 导入/导出 ACL
│   └── 权限继承可视化            → 展示某用户如何获得某权限 (ACL vs 角色 vs 组)
│
├── 🚫 /restrictions             → 封禁与限制管理
│   ├── 主体封禁 (型一)            → 封禁 user/group → 其所有请求 {suspended: true}
│   ├── 资源限制 (型二)            → 对特定 KB/文档设置额外 deny 条件
│   └── 封禁历史                   → 谁在何时被封禁/解除
│
├── 📜 /policies                 → Cerbos 策略管理
│   ├── 策略列表                   → 派生角色策略 + 资源策略 (YAML 源码浏览)
│   ├── 策略版本历史              → Git 式 diff 视图
│   ├── 策略编辑器 (YAML)         → 带语法高亮的编辑器 + 校验
│   └── 策略部署                   → 推送到 Cerbos PDP + 灰度发布
│
├── 🔍 /audit                    → 审计日志
│   ├── 判定记录查询              → 按 decision_id / principal / resource / 时间范围
│   ├── 变更记录查询              → 权限变更历史: 谁 + 什么操作 + 什么资源 + 结果
│   └── 导出                       → CSV / JSON 导出
│
├── 🧪 /playground               → 策略模拟器 (Playground)
│   ├── 输入: principal (JSON) + action + resource (JSON)
│   ├── 输出: allow/deny/indeterminate + reasons + 匹配的策略规则
│   └── 场景预设                   → 预定义的测试场景快速切换
│
└── ⚙️ /settings                 → 管理台设置
    ├── 限流配置                   → /v1/visibility 等端点的 rate limit
    ├── Cerbos PDP 连接配置        → PDP URL + 健康检查
    └── Keycloak 连接配置          → IdP URL + Client Secret
```

### 3.4 关键交互设计

#### 3.4.1 权限授予 Dialog

```
┌─────────────────────────────────────────────────────┐
│  授予权限                                    [✕]    │
│                                                     │
│  主体类型:  [用户 ▼]                                 │
│  主体:      [搜索用户或组...]                        │
│             ┌──────────────────────────────┐        │
│             │ 🧑 张三 (user:zhangsan)       │        │
│             │ 👥 工程组 (group:eng)         │        │
│             │ 🎭 查看者 (role:viewer)       │        │
│             └──────────────────────────────┘        │
│                                                     │
│  资源类型:  [知识库 ▼]                               │
│  资源:      [搜索知识库...]                          │
│             ┌──────────────────────────────┐        │
│             │ 📚 开发测试KB (kb-uuid-1)     │        │
│             │ 📚 产品文档库 (kb-uuid-2)     │        │
│             └──────────────────────────────┘        │
│                                                     │
│  权限:      ☑ kb:read  读取                        │
│             ☐ kb:write 写入                        │
│             ☐ kb:manage 管理                       │
│                                                     │
│  过期时间:  [永不过期 ▼]                             │
│                                                     │
│  备注:      [可选备注...]                            │
│                                                     │
│             [取消]          [授予权限]               │
└─────────────────────────────────────────────────────┘
```

#### 3.4.2 策略模拟器 (Playground)

```
┌─────────────────────────────────────────────────────┐
│  🔬 策略模拟器                                       │
├─────────────────────────────────────────────────────┤
│                                                     │
│  Principal:                    Resource:            │
│  ┌─────────────────────┐     ┌──────────────────┐  │
│  │ {                   │     │ {                │  │
│  │   "id": "user:alice",│     │   "kind": "kb",  │  │
│  │   "roles": ["user"], │     │   "id": "kb-1",  │  │
│  │   "attr": {          │     │   "attr": {       │  │
│  │     "tenant_id":     │     │     "retired":    │  │
│  │       "tenant-dev"   │     │       false       │  │
│  │   }                  │     │   }               │  │
│  │ }                    │     │ }                 │  │
│  └─────────────────────┘     └──────────────────┘  │
│                                                     │
│  Action: [kb:read ▼]                                │
│                                                     │
│  [ 执行模拟 ]                                        │
│                                                     │
│  结果:                                               │
│  ┌─────────────────────────────────────────────┐    │
│  │ ✅ EFFECT_ALLOW                              │    │
│  │                                              │    │
│  │ 匹配规则:                                     │    │
│  │  • resource_policies/kb.yaml:12              │    │
│  │    derivedRoles: [kb_reader]                 │    │
│  │    → 用户通过角色 'user' → 'kb_reader'       │    │
│  │      获得 kb:read                            │    │
│  │                                              │    │
│  │ granted_actions: { "kb-1": ["read"] }        │    │
│  └─────────────────────────────────────────────┘    │
│                                                     │
│  场景预设: [管理员查看自己的KB ▼]  [ 加载 ]          │
└─────────────────────────────────────────────────────┘
```

#### 3.4.3 RAG 系统跳转入口对接

RAG 前端在以下位置提供跳转到管理台的链接，管理台需要支持对应的 URL 参数：

| RAG 跳转位置 | 管理台 URL | 行为 |
|-------------|-----------|------|
| `/settings` → "🔐 权限管理" | `{ADMIN_CONSOLE_URL}/` | 管理台首页 |
| `/kb` → "👥 管理授权" | `{ADMIN_CONSOLE_URL}/resources/kb/{kb_id}` | 直接进入该 KB 的权限管理页 |
| 403 页面 → "前往管理台" | `{ADMIN_CONSOLE_URL}/permissions` | 权限管理页（用于申请） |
| Dashboard → "管理台" | `{ADMIN_CONSOLE_URL}/` | 管理台首页 |

---

## 四、与 IdP (Keycloak) 的集成

### 4.1 Keycloak 需要配置的内容

```
┌────────────────────────────────────────────────────────────┐
│ Keycloak Realm: rag-v14                                    │
├────────────────────────────────────────────────────────────┤
│                                                            │
│ Clients:                                                   │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ 1. rag-frontend        (RAG 前端 → 用户 SSO 登录)     │   │
│ │    • Client Protocol: openid-connect                  │   │
│ │    • Access Type: public                              │   │
│ │    • Redirect URIs: http://192.168.1.127:3001/*       │   │
│ │    • Scopes: openid, profile, email, roles            │   │
│ ├──────────────────────────────────────────────────────┤   │
│ │ 2. admin-console       (管理台 → 管理员 SSO 登录)     │   │
│ │    • Client Protocol: openid-connect                  │   │
│ │    • Access Type: confidential                        │   │
│ │    • Redirect URIs: http://192.168.1.127:3002/*       │   │
│ ├──────────────────────────────────────────────────────┤   │
│ │ 3. permission-service  (权限服务后端 → 服务间认证)     │   │
│ │    • Client Protocol: openid-connect                  │   │
│ │    • Access Type: confidential                        │   │
│ │    • Service Account: Enabled                         │   │
│ │    • Scopes: openid, roles, groups                    │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                            │
│ Roles (Realm Roles):                                       │
│   • system_admin  — 超级管理员                             │
│   • user          — 普通用户 (默认角色)                     │
│                                                            │
│ Groups:                                                    │
│   • /engineering                                          │
│   • /product                                              │
│   • /admin                                                │
│                                                            │
│ Client Scopes / Mappers:                                  │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ JWT Claims 配置:                                      │   │
│ │   "sub":    用户 ID                                   │   │
│ │   "tenant": 租户 ID (从用户属性映射)                   │   │
│ │   "roles":  ["system_admin" | "user"]                 │   │
│ │   "groups": ["engineering", "product"]                │   │
│ │   "email":  用户邮箱                                   │   │
│ │   "preferred_username": 用户名                        │   │
│ └──────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────┘
```

### 4.2 权限服务与 Keycloak 的数据同步

权限服务后端**不维护用户/组数据**——只读同步自 Keycloak：

```
Keycloak                       权限服务后端
   │                               │
   │  定时同步 (每 15 分钟)         │
   │  GET /admin/realms/{r}/users  │
   │ ────────────────────────────► │ → 更新本地 users 缓存表
   │                               │   (id, username, email, tenant, groups, roles)
   │  GET /admin/realms/{r}/groups │
   │ ────────────────────────────► │ → 更新本地 groups 缓存表
   │                               │   (id, name, members[])
```

这样管理台可以展示用户/组列表而无需每次调 Keycloak API，且用户/组数据在管理台的权限授予 Dialog 中可作为选项。

---

## 五、事件系统设计

### 5.1 VisibilityChanged 事件

权限服务后端在 ACL/角色绑定/限制发生变更时，发布 `VisibilityChanged` 事件：

```
┌──────────────────────────────┐
│ 权限变更触发                  │
│ (grant / revoke / bind /     │
│  restriction add/remove)     │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ 权限服务后端                   │
│ 1. 写数据库 (ACL/role/         │
│    restriction 表)            │
│ 2. 递增 global_permission_    │
│    version                    │
│ 3. INSERT permission_changes  │
│ 4. 发布 Redis Pub/Sub 事件    │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────────┐
│ Redis Pub/Sub                                             │
│ Channel: "visibility_changed"                             │
│ Payload:                                                  │
│   {                                                       │
│     "event_id": "uuid",                                   │
│     "event_type": "VisibilityChanged",                    │
│     "resource": {"type": "kb", "id": "kb-uuid"},          │
│     "channel": {"kb": "kb-uuid"},    // KB 粒度聚合       │
│     "tenant": "tenant-dev",                               │
│     "version": 12345,                                     │
│     "unmounted": false,                                   │
│     "change_detail": {                                    │
│       "action": "acl_granted",                            │
│       "principal": "user:zhangsan",                       │
│       "permission": "kb:read"                             │
│     },                                                    │
│     "occurred_at": "2026-07-30T12:00:00Z"                 │
│   }                                                       │
└────────────┬─────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ RAG v14 P-AUTHC              │
│ (visibility_events.py 订阅)  │
│                              │
│ 1. 幂等去重 (event_id)       │
│ 2. 如果是 KB 粒度:            │
│    expand_visibility_changed_ │
│    task(kb_id)               │
│    → 分页查本地               │
│      document_kb_mounts      │
│    → 各 (doc, kb) 提交       │
│      stamp_channel_task      │
│ 3. 如果是文档粒度:            │
│    直接提交 stamp_channel_   │
│    task(doc_id, kb_id)       │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ B-INGEST stamp_channel_task  │
│ POST /v1/visibility          │
│ → upsert Milvus chunk stamps │
└──────────────────────────────┘
```

### 5.2 事件可靠性保证

| 保证 | 实现 |
|------|------|
| **至少一次投递** | Redis Pub/Sub + 消费者 ack 机制 (或 Redis Stream) |
| **幂等消费** | 消费者按 event_id 去重 |
| **事件持久化** | permission_changes 表 (PostgreSQL) 持久化所有变更 |
| **事件重放** | 消费者可从指定 version 开始补消费 |
| **对账兜底** | RAG 侧定期对账 (stamp_drift/orphan_stamp/mirror_gap) |

---

## 六、部署架构

### 6.1 服务组件

```
┌─────────────────────────────────────────────────────────────┐
│ 外部系统 Docker Compose (独立于 RAG 系统)                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  permission-service:    # 权限服务后端 (FastAPI)             │
│    image: permission-service:latest                         │
│    ports: ["127.0.0.1:18080:8080"]                          │
│    environment:                                             │
│      DATABASE_URL=postgresql://...                          │
│      CERBOS_PDP_URL=http://cerbos:3592                      │
│      KEYCLOAK_URL=http://keycloak:8080                      │
│      REDIS_URL=redis://redis:6379                           │
│    depends_on: [postgres, cerbos, redis]                    │
│                                                             │
│  admin-console:         # 管理台前端 (Next.js)               │
│    build: ./admin-console                                   │
│    ports: ["3002:3000"]                                     │
│    environment:                                             │
│      PERMISSION_SERVICE_URL=http://permission-service:8080  │
│      KEYCLOAK_URL=http://192.168.1.127:8080                 │
│                                                             │
│  cerbos:                # Cerbos PDP (已有)                  │
│    image: ghcr.io/cerbos/cerbos:0.39.0                      │
│    ports: ["13592:3592", "13593:3593"]                      │
│    volumes: [./cerbos/policies:/policies]                   │
│                                                             │
│  postgres:              # 权限服务独立数据库                  │
│    image: postgres:16-alpine                                │
│    ports: ["25433:5432"]                                    │
│    volumes: [permission_pgdata:/var/lib/postgresql/data]    │
│                                                             │
│  redis:                 # 事件发布 (可复用 RAG 的 Redis)      │
│    image: redis:7-alpine                                    │
│    ports: ["16380:6379"]                                    │
│                                                             │
│  keycloak:              # IdP (已有)                          │
│    image: quay.io/keycloak/keycloak:25.0                    │
│    ports: ["8080:8080"]                                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 端口规划

| 服务 | 内部端口 | 宿主机端口 | 说明 |
|------|---------|-----------|------|
| permission-service | 8080 | 18080 | 权限服务后端 API |
| admin-console | 3000 | 3002 | 管理台前端 |
| cerbos | 3592/3593 | 13592/13593 | PDP (已有) |
| postgres | 5432 | 25433 | 权限服务独立 DB |
| redis | 6379 | 16380 | 事件 Pub/Sub |
| keycloak | 8080 | 8080 | IdP (已有) |

---

## 七、迁移路径：从本地镜像到外部服务

### 7.1 当前状态

RAG 系统的 `cerbos_client.py` 使用本地 PostgreSQL 表 (`resource_registry`, `mount_registry`) 作为资源镜像，并直接调用 Cerbos PDP HTTP API。这是**开发模式**的实现。

### 7.2 迁移阶段

```
阶段 0: 开发模式 (当前)
  cerbos_client.py
  ├── 决策面: 直接调 Cerbos HTTP API ✅
  ├── 投影面: 本地 DB 查询 ✅
  └── 管理面: 本地 DB INSERT/UPDATE ✅

阶段 1: 权限服务后端上线
  1. 部署 permission-service + 数据库
  2. 迁移 resource_registry / mount_registry 数据到权限服务
  3. cerbos_client.py 改为 HTTP 客户端:
     ├── 决策面: 调 permission-service /v1/check 等
     ├── 投影面: 调 permission-service /v1/prefilter, /v1/visibility
     └── 管理面: 调 permission-service /v1/resources/register 等
  4. 引入功能开关: AUTHZ_SERVICE_MODE=local|remote
     (local: 当前实现; remote: 调外部服务)

阶段 2: 管理台上线
  1. 部署 admin-console
  2. 配置 RAG 系统 .env: ADMIN_CONSOLE_URL=http://192.168.1.127:3002
  3. 在管理台中配置初始权限:
     - 将现有 owner → 转换为 ACL entries
     - 将 system_admin → 绑定 admin 派生角色
  4. 验证: RAG 前端跳转链接全部指向管理台

阶段 3: 事件系统对接
  1. permission-service 发布 VisibilityChanged 到 Redis Pub/Sub
  2. RAG P-AUTHC 订阅 Redis Pub/Sub (替换当前本地轮询)
  3. 验证: 权限变更 → 盖戳自动刷新

阶段 4: 生产加固
  1. 关闭 AUTHZ_SERVICE_MODE=local，强制走 remote
  2. 配置熔断/限流/重试策略
  3. 跑联合契约测试 J-1~J-20
  4. 监控指标: authz_call_duration_seconds, authz_call_failed_total
```

### 7.3 cerbos_client.py 改造示意

```python
# 当前实现 (阶段 0)
class CerbosClient:
    def get_prefilter(self, ...):
        # 直接查本地 DB
        rows = await conn.fetch("SELECT id FROM knowledge_bases...")
        # 直接调 Cerbos HTTP API
        results = self.check_batch(...)
        return {"kbs": allowed_kbs}

# 阶段 1: 引入 HTTP 客户端
class PermissionServiceClient:
    """HTTP client for the external permission service."""
    
    def __init__(self, base_url: str):
        self.base_url = base_url
        self._http = httpx.Client(timeout=30.0)
    
    def get_prefilter(self, request_id, credential):
        resp = self._http.get(
            f"{self.base_url}/v1/prefilter",
            params={"credential": credential},
            headers={"X-Client-Id": "retrieval", "X-Request-Id": request_id},
        )
        resp.raise_for_status()
        return resp.json()
    
    # ... check, filter, visibility, context, lifecycle ports 同理

# cerbos_client.py 中的 get_client()
# 改为返回 PermissionServiceClient 或 CerbosClient
# 取决于 AUTHZ_SERVICE_MODE 配置
```

### 7.4 RAG 系统配置变更

```bash
# .env 新增
AUTHZ_SERVICE_MODE=local              # local | remote
AUTHZ_SERVICE_URL=http://192.168.1.127:18080  # 权限服务后端地址
AUTHZ_EVENT_STREAM_URL=redis://:16380          # VisibilityChanged 事件订阅
ADMIN_CONSOLE_URL=http://192.168.1.127:3002    # 管理台地址
```

---

## 八、实施优先级

### P0 — 权限服务后端核心 API

| # | 功能 | 说明 |
|---|------|------|
| 1 | 数据模型建表 | resource_registry, mount_registry, acl_entries, role_bindings, restrictions, permission_changes |
| 2 | `/v1/check` 端点 | 单条判定，含 Cerbos PDP 调用 + 三态映射 |
| 3 | `/v1/prefilter` 端点 | 检索前编译，含型一封禁检查 |
| 4 | `/v1/visibility` 端点 | 可见性投影，含 ACL 聚合 + 版本号 |
| 5 | `/v1/filter` 端点 | 批量 doc:retrieve 判定 |
| 6 | `/v1/context` 端点 | ctx_token 铸造 |
| 7 | 生命周期端口 (4 个) | register/link/unlink/retire |
| 8 | RAG 侧 cerbos_client.py 改造 | HTTP 客户端 + AUTHZ_SERVICE_MODE 开关 |

### P1 — 管理台基础功能

| # | 功能 | 说明 |
|---|------|------|
| 9 | 管理台登录 + SSO | 管理员通过 Keycloak 登录 |
| 10 | 资源浏览 (KB/文档列表) | 同步自 resource_registry |
| 11 | 权限授予/回收 Dialog | ACL CRUD |
| 12 | 角色绑定管理 | Role bindings CRUD |
| 13 | 用户/组浏览 (只读) | 同步自 Keycloak |
| 14 | 管理台管理 API | `/api/v1/acl/*`, `/api/v1/roles/*` 等 |

### P2 — 事件系统 + 对账

| # | 功能 | 说明 |
|---|------|------|
| 15 | VisibilityChanged 事件发布 | Redis Pub/Sub |
| 16 | RAG 侧事件订阅对接 | P-AUTHC visibility_events.py 改订阅 Redis |
| 17 | 全局版本号机制 | permission_changes + global_permission_version |
| 18 | 事件重放/补消费 | 从指定 version 重新消费 |

### P3 — 完整体验

| # | 功能 | 说明 |
|---|------|------|
| 19 | 策略管理 (YAML 编辑器) | Cerbos 策略的编辑/版本/发布 |
| 20 | 策略模拟器 (Playground) | 沙箱判定 |
| 21 | 审计日志查询 | 按 decision_id / principal / resource 查询 |
| 22 | 封禁/限制管理 | 型一 + 型二 限制 CRUD |
| 23 | 权限继承可视化 | 展示权限来源链路 |
| 24 | 批量导入/导出 ACL | CSV 格式 |

---

## 九、文件索引

```
外部系统代码仓库 (建议独立 git 仓库: permission-system)
│
├── permission-service/           # 权限服务后端
│   ├── main.py                   # FastAPI 入口
│   ├── config.py                 # 配置
│   ├── models/                   # SQLAlchemy 数据模型
│   │   ├── acl.py
│   │   ├── role_binding.py
│   │   ├── restriction.py
│   │   ├── resource.py
│   │   └── change_log.py
│   ├── api/                      # API 路由
│   │   ├── decision.py           # /v1/check, /v1/filter
│   │   ├── projection.py         # /v1/prefilter, /v1/visibility
│   │   ├── context.py            # /v1/context
│   │   ├── lifecycle.py          # /v1/resources/register|link|unlink|retire
│   │   ├── acl.py                # /api/v1/acl/*
│   │   ├── roles.py              # /api/v1/roles/*
│   │   ├── restrictions.py       # /api/v1/restrictions/*
│   │   └── audit.py              # /api/v1/audit/*
│   ├── services/                 # 业务逻辑
│   │   ├── cerbos_adapter.py     # Cerbos PDP 调用适配
│   │   ├── acl_resolver.py       # ACL + 角色 → 有效权限解析
│   │   ├── stamp_calculator.py   # 戳记计算 (visibility)
│   │   └── event_publisher.py    # Redis Pub/Sub 事件发布
│   ├── idp/                      # IdP 集成
│   │   └── keycloak_sync.py      # Keycloak 用户/组同步
│   ├── migrations/               # Alembic 数据库迁移
│   └── tests/                    # 测试
│
├── admin-console/                # 管理台前端
│   ├── app/                      # Next.js App Router 页面
│   │   ├── login/
│   │   ├── dashboard/
│   │   ├── resources/
│   │   ├── users-groups/
│   │   ├── permissions/
│   │   ├── restrictions/
│   │   ├── policies/
│   │   ├── audit/
│   │   ├── playground/
│   │   └── settings/
│   ├── components/               # 共享组件
│   ├── lib/                      # API 客户端
│   └── stores/                   # 状态管理
│
├── cerbos/                       # Cerbos 策略 (从 RAG 项目复制或共享)
│   └── policies/
│       ├── derived_roles/
│       └── resource_policies/
│
├── docker-compose.yml            # 外部系统 Docker Compose
└── README.md
```

---

> **总结**：外部系统由权限服务后端 (Python/FastAPI) + 管理台前端 (Next.js) + Cerbos PDP + Keycloak 四方组成。权限服务后端是 ACL/角色绑定/限制的权威存储和 Cerbos PDP 的适配层，提供 RAG 系统所需的全部 5 个决策/投影端点和 4 个生命周期端口。管理台前端是权限管理员的操作界面，RAG 系统通过 `ADMIN_CONSOLE_URL` 跳转进入。迁移路径从当前的本地 DB 镜像 (cerbos_client.py) 通过 `AUTHZ_SERVICE_MODE` 开关逐步切换到外部服务，接口签名保持不变（防腐层的价值）。
