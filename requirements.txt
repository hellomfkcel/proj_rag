# ========================================================
# requirements.txt — 全量依赖（dev / CI / 本地 conda 环境）
#
# 按镜像分层拆分（生产构建见 Dockerfile 的 multi-stage，逐层安装）：
#   requirements-base.txt      真共享依赖 — visibility-events（及所有层基座）
#   requirements-milvus.txt    直连 Milvus + JWT — stamping/outbox（及 web/haystack 基座）
#   requirements-web.txt       API 特有 — api
#   requirements-haystack.txt  Haystack 检索/摄入栈（无 torch）— ingestion/retrieval
#   requirements-gpu.txt       模型加载栈 + web 服务器 — 仅 embedding-service
#
# ⚠️ torch GPU 版本请单独安装（不在以上任何文件中，由 Dockerfile 离线装）：
#   pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cu130
# Python 3.11 | GPU: RTX 3060 | CUDA: 13.2
# Langfuse Server: langfuse/langfuse:3
# ========================================================

-r requirements-base.txt
-r requirements-milvus.txt
-r requirements-web.txt
-r requirements-haystack.txt
-r requirements-gpu.txt
