server:
  httpListenAddr: ":3592"
  grpcListenAddr: ":3593"

storage:
  driver: disk
  disk:
    directory: /policies
    watchForChanges: true

schema:
  enforcement: warn

audit:
  enabled: true
  backend: local
  local:
    storagePath: /audit
    retentionPeriod: 168h
